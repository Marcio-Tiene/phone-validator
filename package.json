{
  "name": "phone-validator",
  "version": "0.0.1",
  "description": "Library to validate phone numbers",
  "main": "index.ts",
  "author": "Marcio Tiene",
  "license": "MIT",
  "private": false,
  "scripts": {
    "dev": "ts-node-dev --respawn --watch --transpile-only index.ts",
    "run:file": "ts-node-dev --watch --transpile-only"
  },
  "devDependencies": {
    "ts-node": "^10.3.1",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.4"
  }
}
